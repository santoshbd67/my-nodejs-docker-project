pipeline {
    agent any
    stages{
    stage("checkout"){
        steps{
            checkout scm
        }
    }
        stage("Test") {
            steps{
                 sh 'yum install npm'
                 sh 'snpm test'
            }
        }
        stage("Build") {
            steps {
                sh 'npm run build'
            }
        }
    }
}
