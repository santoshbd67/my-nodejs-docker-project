pipeline{
    agent any
    stages("checkout"){
        steps{
            checkout scm
        }
    }
        stage("Test"){
            steps{
                 sh 'sudo yum npm install'
                 sh 'npm test'
            }
        }
        stage("Build"){
            steps{
                sh 'npm run build'
            }
        }
}
